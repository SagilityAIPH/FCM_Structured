# Database Rules

- Check constraints, indexes, nullability, defaults, and migration ordering.
- Avoid destructive migrations without approval and backup/rollback planning.
- Consider data volume, lock duration, and backfill behavior.
- Keep PHI classification visible in schema and logging decisions.
- Verify migrations in a safe environment before release.

