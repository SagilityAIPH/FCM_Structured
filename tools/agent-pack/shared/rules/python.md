# Python Rules

- Prefer standard library and existing project dependencies.
- Use explicit errors and observable logs without sensitive values.
- Add dry-run paths for automation that changes external state.
- Keep selectors, waits, and retries intentional.
- Verify with tests, safe sample data, or documented simulation.

