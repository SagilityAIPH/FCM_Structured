# Frontend Rules

- Match the existing design system and component patterns.
- Build the real workflow first, not a marketing shell.
- Include loading, empty, disabled, and error states.
- Verify responsive layout and accessible labels.
- Do not use real PHI in UI fixtures, screenshots, local storage, or analytics.

