# Claude Adapter

Copy this file to a target repo as `CLAUDE.md`. It delegates behavior to `AGENTS.md`, uses `.claude/agents` for specialized agents, `.claude/commands` for slash wrappers, and `.claude/skills` for reusable procedures.

