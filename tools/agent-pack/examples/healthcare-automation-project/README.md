# Healthcare Automation Project Example

Use `healthcare-ops-sme-us` with PHI-safe synthetic data. Route implementation to Python, backend, or QA agents after workflow assumptions and PHI touchpoints are documented.

