# Smoke Tests

1. Install into an empty temporary project.
2. Confirm `AGENTS.md` exists.
3. Confirm Claude mode creates `.claude/agents`, `.claude/commands`, and `.claude/skills`.
4. Confirm Codex mode creates `.agents/skills`.
5. Run `python installers/doctor.py` in this repository.

